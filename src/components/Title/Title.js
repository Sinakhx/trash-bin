import React from "react";
import PropTypes from "prop-types";

export const Title = (props) => {
  return <div className="Title-container">Hello from Title</div>;
};

Title.propTypes = {
  props: PropTypes.object,
};
